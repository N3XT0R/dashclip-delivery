<?php

namespace Deployer;

require 'deploy_laravel_config.php';

// Project
set('application', 'dashclip-delivery');

// YOUR github PAT in clear text
set('GITHUB_PAT', '');

// Repository with PAT in the URL
set('repository', sprintf(
    'https://%s@github.com/N3XT0R/video_sharing.git',
    get('GITHUB_PAT')
));

set('branch', 'master');
set('git_tty', true);
set('keep_releases', 5);
set('allow_anonymous_stats', false);

// Laravel specifics
add('shared_files', ['.env']);
add('shared_dirs', ['storage']);
add('writable_dirs', ['storage', 'bootstrap/cache', 'storage/app/public/uploads/tmp']);
set('writable_mode', 'chmod');
set('writable_chmod_mode', '0775');
set('writable_chmod_recursive', true);
set('http_user', 'www-data');
set('git_tty', false);
set('ssh_type', 'native');

// Hosts
host('prd')
    ->setHostname('example.tld')
    ->set('php_version', '8.4')
    ->setRemoteUser('root')
    ->set('deploy_path', '/var/www/dashclip')
    ->set('ssh_arguments', ['-o AddressFamily=inet6', '-o ConnectTimeout=30'])
    ->set('systemd', [
            'units' => ['php8.4-fpm', 'laravel-reverb'],
            'action' => 'restart',
            'sudo' => true,
        ])
    ->setSshMultiplexing(false);

// Hooks
after('deploy:failed', 'deploy:unlock');
after('deploy:cleanup', 'artisan:up');
// Passport encryption keys live in the shared storage dir and must persist across
// releases. 'artisan:passport:keys' runs `passport:keys` without --force, which is
// idempotent-safe: Laravel skips generation if the keys already exist, so previously
// issued tokens/sessions are never invalidated by a later deploy.
after('deploy:vendors', 'artisan:passport:keys');
// storage/api-docs is gitignored and l5-swagger's generate_always is false, so both
// documentation specs must be (re)generated on every deploy (--all covers every
// documentation in config/l5-swagger.php).
after('deploy:vendors', 'artisan:l5-swagger:generate');
